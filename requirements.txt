pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
fastapi>=0.100
uvicorn>=0.23

# Optional live-data extras (the system falls back to offline/synthetic mode without them):
# yfinance>=0.2        # live OHLCV prices
# feedparser>=6.0      # RSS news ingestion
# transformers, torch  # FinBERT sentiment (lexicon scorer used by default)

# Development
pytest>=7.0
httpx>=0.24
