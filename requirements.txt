pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
fastapi>=0.100
uvicorn>=0.23

# Live market data (Yahoo Finance, NSE, RSS) uses only the standard library —
# no extra dependencies. Optional upgrade:
# transformers, torch  # FinBERT sentiment (lexicon scorer used by default)

# Development
pytest>=7.0
httpx>=0.24
